import React, { Component } from 'react';

export default class Emails extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>Emails Page</div>
    );
  }
}
